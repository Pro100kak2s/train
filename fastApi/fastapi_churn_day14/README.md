В текущей версии API расположен в main.py, а бизнес-логика — в services/dataset_service.py. При дальнейшем развитии проекта возможен перенос маршрутов в отдельный модуль api, а конфигурации — в core или config.